<% include ../Templates/header.ejs %>

<div class="container" ng-controller="authCtrl">
<div class="col-sm-6 col-sm-offset-3">
    <h1><span class="fa fa-sign-in"></span> Login</h1>
    <!-- show any messages that come back with authentication -->
    <% if (message.length > 0) { %>
        <div class="alert alert-danger"><%= message %></div>
    <% } %>

    <!-- Sign Up FORM -->
    <form novalidate ng-submit="login()">
        <h1>Account</h1>
        <div class="form-group">
            <label>Email</label>
            <input type="email" class="form-control" ng-model="email" placeholder="user@domain.com" required/>
        </div>
        <div class="form-group">
            <label>Password</label>
            <input type="password" class="form-control" ng-model="password" placeholder="Password" required/>
        </div>
        <button type="submit" class="btn btn-primary btn-lg pull-right">Login</button><br/><br/>
    </form>
    <hr>
    <p>Don't have an account? <a href="/login">Signup</a></p>

</div>

</div>

</body>
</html>
